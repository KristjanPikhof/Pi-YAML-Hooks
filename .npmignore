# Exclude test files from published tarball
src/**/*.test.ts
src/core/*.test.ts
src/pi/*.test.ts

# Exclude TypeScript build info files
*.tsbuildinfo

# Exclude large example scripts (Python workers) from tarball
examples/atomic-commit-snapshot-worker/*.py
